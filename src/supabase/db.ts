export type IssuedBooksResult = {
  id: number;
  student_name: string;
  title: string;
  issue_date: any;
  due_date: any;
};

export type BooksResult = {
  id: number;
  title: string;
  author: string;
  isbn: number;
};
